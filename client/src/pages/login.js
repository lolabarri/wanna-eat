import React from 'react';
import { LoginForm } from '../components/LoginForm';

export const Login = ({history}) => {
    return (
        <React.Fragment>
            <LoginForm/>
        </React.Fragment>
    )
};